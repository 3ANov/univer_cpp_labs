#include "header.h"

int main(){
    double x, y;
    x = 0;
    y = 0;

    // cin >> x;
    // cin >> y;

    // cout << f1(x, y) << '\n';
    // cout << f2(x, y) << '\n';
    // f3(-3.0, 0.0, 0.1);
    f4();

    return 0;
}
